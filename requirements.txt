# Supervised path-integration model (model.py, train.py, evaluate.py, scores.py,
# figures.py, dataset.py) plus notebooks 00-05. See README.md.
#
# torch: PyTorch 2.7+ is the minimum for Blackwell/RTX 50-series (sm_120) GPU
# support; earlier versions will import fine but CUDA ops will fail on that
# hardware. No special --index-url is needed -- the pinned version below
# resolves from the default PyPI index.
torch==2.13.0+cu130
numpy==2.5.2
pandas==3.0.5
matplotlib==3.11.1
scipy==1.18.0
scikit-learn==1.9.0

# Only needed to regenerate trajectories from scratch via
# notebooks/00_make_dataset.ipynb. Not required for training/evaluation against
# an already-generated data/ directory.
ratinabox
